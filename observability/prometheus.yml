global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'nest-better-auth'
    static_configs:
      - targets: ['host.docker.internal:3000']
    metrics_path: /metrics
    # If running app in compose with service name 'api':
    # - targets: ['api:3000']
